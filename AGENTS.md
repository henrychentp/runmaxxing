# Agents

Load `SKILL.md` before writing adapters or changing inputs.

The user-facing contract is `schemas/` plus the example files in `data/`.

Run `runmaxxing-check`, then `runmaxxing`. Do not choose the session.
